# CodeX Portfolio - Admin Dashboard Setup Guide

## 🎯 ภาพรวม

ระบบหลังบ้าน (Admin Dashboard) สำหรับจัดการข้อมูลทั้งหมดบนเว็บไซต์ Portfolio ผ่าน Supabase Database

## 📋 ขั้นตอนการติดตั้ง

### 1. ติดตั้ง Dependencies

Dependencies ได้ถูกติดตั้งแล้ว:

- `@supabase/supabase-js` - Supabase Client Library

### 2. ตั้งค่า Supabase Database

1. เข้าไปที่ [Supabase Dashboard](https://supabase.com/dashboard)
2. เลือกโปรเจค: `tncaevvnicaygulracft`
3. ไปที่ **SQL Editor**
4. คัดลอกและรันคำสั่ง SQL จากไฟล์ `supabase-schema.sql`

SQL Schema นี้จะสร้าง:

- ✅ ตาราง `personal_info` - ข้อมูลส่วนตัว
- ✅ ตาราง `universities` - ข้อมูลมหาวิทยาลัย
- ✅ ตาราง `projects` - โครงการทั้งหมด
- ✅ ตาราง `education` - ประวัติการศึกษา
- ✅ ตาราง `experience` - ประสบการณ์การทำงาน
- ✅ Indexes สำหรับ Performance
- ✅ Triggers สำหรับ Auto-update timestamps
- ✅ Row Level Security (RLS) Policies
- ✅ ข้อมูลตัวอย่าง (Sample Data)

### 3. ตรวจสอบไฟล์ Environment

ไฟล์ `.env` ได้ถูกสร้างแล้วพร้อม Supabase credentials:

```env
VITE_SUPABASE_URL=https://tncaevvnicaygulracft.supabase.co
VITE_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
```

## 🚀 การใช้งาน

### เข้าถึง Admin Dashboard

1. รัน Development Server:

```bash
npm run dev
```

2. เปิดเบราว์เซอร์และไปที่:

```
http://localhost:5173/admin
```

### ฟีเจอร์ของ Admin Dashboard

#### 📝 ข้อมูลส่วนตัว (Personal Info)

- แก้ไขชื่อ-นามสกุล (ไทย/อังกฤษ)
- อัพเดทตำแหน่งงาน
- จัดการ Bio/ประวัติย่อ
- เพิ่ม/ลบทักษะ (Skills)
- อัพเดทข้อมูลติดต่อ (Email, LinkedIn, GitHub, LINE)
- จัดการคะแนน IELTS

#### 🚀 โครงการ (Projects)

- ✅ เพิ่มโครงการใหม่
- ✏️ แก้ไขโครงการ
- 👁️ แสดง/ซ่อนโครงการ
- 🗑️ ลบโครงการ
- 🔢 จัดเรียงลำดับการแสดง
- 🏷️ จัดการ Tags

#### 🎓 การศึกษา (Education)

- ✅ เพิ่มประวัติการศึกษา
- ✏️ แก้ไขข้อมูล
- 👁️ แสดง/ซ่อนรายการ
- 🗑️ ลบรายการ
- 🔢 จัดเรียงลำดับ
- 🏅 เพิ่ม Badge (เช่น "In Progress", "Graduated")

#### 💼 ประสบการณ์ (Experience)

- ✅ เพิ่มประสบการณ์การทำงาน
- ✏️ แก้ไขข้อมูล
- 👁️ แสดง/ซ่อนรายการ
- 🗑️ ลบรายการ
- 🔢 จัดเรียงลำดับ
- 🏅 เพิ่ม Badge (เช่น "Leadership", "International")

## 📊 โครงสร้างฐานข้อมูล

### Personal Info Table

```sql
- id (UUID)
- name_th, name_en
- nickname
- title_th, title_en
- bio_th, bio_en
- email
- linkedin_url, github_url, line_id
- ielts_score, ielts_validity
- skills (Array)
- created_at, updated_at
```

### Projects Table

```sql
- id (UUID)
- title
- description_th, description_en
- url
- icon
- tags (Array)
- order_index
- is_visible (Boolean)
- created_at, updated_at
```

### Education Table

```sql
- id (UUID)
- year
- title_th, title_en
- subtitle_th, subtitle_en
- description_th, description_en
- university_id (FK)
- badge
- order_index
- is_visible (Boolean)
- created_at, updated_at
```

### Experience Table

```sql
- id (UUID)
- year
- title_th, title_en
- subtitle_th, subtitle_en
- description_th, description_en
- badge
- order_index
- is_visible (Boolean)
- created_at, updated_at
```

## 🔒 Security (Row Level Security)

RLS Policies ได้ถูกตั้งค่าแล้ว:

- ✅ **Public Read Access** - ทุกคนสามารถอ่านข้อมูลที่ `is_visible = true`
- ⚠️ **Admin Write Access** - ต้องตั้งค่า Authentication เพิ่มเติม

### การเพิ่ม Authentication (ขั้นตอนถัดไป)

หากต้องการป้องกันหน้า Admin:

1. ตั้งค่า Supabase Auth
2. สร้าง Login Page
3. เพิ่ม Protected Route
4. อัพเดท RLS Policies สำหรับ Admin Role

## 🎨 Components ที่สร้าง

```
src/
├── lib/
│   └── supabase.ts              # Supabase Client & Types
├── pages/
│   └── Admin.tsx                # Admin Dashboard Page
└── components/
    └── admin/
        ├── ProjectForm.tsx      # Form สำหรับจัดการโครงการ
        ├── EducationForm.tsx    # Form สำหรับจัดการการศึกษา
        ├── ExperienceForm.tsx   # Form สำหรับจัดการประสบการณ์
        └── PersonalInfoForm.tsx # Form สำหรับจัดการข้อมูลส่วนตัว
```

## 📝 การใช้งานข้อมูลจาก Database

### ตัวอย่างการดึงข้อมูลโครงการ

```typescript
import { supabase } from "@/lib/supabase";

// ดึงโครงการทั้งหมดที่แสดง
const { data: projects } = await supabase
  .from("projects")
  .select("*")
  .eq("is_visible", true)
  .order("order_index", { ascending: true });
```

### ตัวอย่างการอัพเดทข้อมูล

```typescript
// อัพเดทโครงการ
const { error } = await supabase
  .from("projects")
  .update({ title: "New Title" })
  .eq("id", projectId);
```

## 🔄 การ Migrate ข้อมูลเดิม

ข้อมูลตัวอย่างจาก `Index.tsx` ได้ถูกเพิ่มลงใน Database แล้ว คุณสามารถ:

1. เข้า Admin Dashboard
2. แก้ไขข้อมูลตามต้องการ
3. อัพเดท `Index.tsx` ให้ดึงข้อมูลจาก Database แทน hardcode

### ตัวอย่างการแก้ไข Index.tsx

```typescript
// Before (Hardcoded)
const projects = [
  { title: "...", description: "..." },
  // ...
];

// After (From Database)
const [projects, setProjects] = useState([]);

useEffect(() => {
  const fetchProjects = async () => {
    const { data } = await supabase
      .from("projects")
      .select("*")
      .eq("is_visible", true)
      .order("order_index");
    setProjects(data || []);
  };
  fetchProjects();
}, []);
```

## 🎯 Next Steps

1. ✅ รัน SQL Schema ใน Supabase
2. ✅ ทดสอบ Admin Dashboard
3. 🔄 อัพเดท Index.tsx ให้ดึงข้อมูลจาก Database
4. 🔒 เพิ่ม Authentication สำหรับ Admin (ถ้าต้องการ)
5. 🚀 Deploy และทดสอบ

## 💡 Tips

- ใช้ `order_index` เพื่อจัดเรียงลำดับการแสดง
- ใช้ `is_visible` เพื่อซ่อน/แสดงรายการโดยไม่ต้องลบ
- Tags และ Skills เป็น Array สามารถเพิ่มได้หลายค่า
- ข้อมูลทุกอย่างรองรับทั้งภาษาไทยและอังกฤษ

## 🐛 Troubleshooting

### ปัญหา: ไม่สามารถเชื่อมต่อ Supabase

- ตรวจสอบ `.env` มี credentials ถูกต้อง
- ตรวจสอบ Supabase Project ยังใช้งานได้

### ปัญหา: ไม่สามารถบันทึกข้อมูล

- ตรวจสอบ RLS Policies ใน Supabase
- ตรวจสอบ Console สำหรับ Error Messages

### ปัญหา: ข้อมูลไม่แสดง

- ตรวจสอบ `is_visible = true`
- ตรวจสอบ SQL Schema ถูกรันครบทุกคำสั่ง

---

สร้างโดย CodeX 🚀

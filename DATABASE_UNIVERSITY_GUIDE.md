# คู่มือการใช้งานระบบจัดการข้อมูลมหาวิทยาลัยจาก Database

## 📋 สรุปการเปลี่ยนแปลง

ระบบได้ถูกปรับปรุงให้ดึงข้อมูลทั้งหมดจาก Supabase Database แทนการ hardcode ใน code

### ✅ สิ่งที่เปลี่ยนแปลง

1. **สร้างตาราง `map_universities`** ใน Supabase
2. **ลบ hardcoded data** ออกจาก ThailandEducationMap.tsx
3. **เพิ่ม dynamic data fetching** จาก database
4. **อัพเดท UI** ให้แสดงข้อมูลคณะและสาขาอย่างถูกต้อง

## 🚀 ขั้นตอนการติดตั้ง

### 1. รัน SQL Script

เข้าไปที่ Supabase Dashboard → SQL Editor และรันคำสั่งจากไฟล์:

```bash
supabase_map_universities.sql
```

Script นี้จะ:

- สร้างตาราง `map_universities`
- ตั้งค่า RLS (Row Level Security)
- Insert ข้อมูลเริ่มต้นของมหาวิทยาลัยทั้ง 4 แห่ง

### 2. ตรวจสอบข้อมูล

ตรวจสอบว่าข้อมูลถูก insert เรียบร้อยแล้ว:

```sql
SELECT * FROM map_universities ORDER BY order_index;
```

ควรเห็นข้อมูล 4 แถว:

- CMU (north)
- KKU (northeast)
- RU (central)
- PSU (south)

## 📊 โครงสร้างตาราง map_universities

| Column       | Type         | Description              | Example                                  |
| ------------ | ------------ | ------------------------ | ---------------------------------------- |
| id           | UUID         | Primary key              | auto-generated                           |
| region       | VARCHAR(50)  | รหัสภาค                  | 'north', 'northeast', 'central', 'south' |
| name_th      | VARCHAR(255) | ชื่อมหาวิทยาลัย (ไทย)    | 'มหาวิทยาลัยขอนแก่น'                     |
| name_en      | VARCHAR(255) | ชื่อมหาวิทยาลัย (อังกฤษ) | 'Khon Kaen University'                   |
| region_th    | VARCHAR(100) | ชื่อภาค (ไทย)            | 'ภาคอีสาน'                               |
| year         | VARCHAR(100) | ปีการศึกษา               | '2025 - ปัจจุบัน'                        |
| degree_level | VARCHAR(100) | ระดับการศึกษา            | 'ปริญญาโท (M.Ed.)'                       |
| faculty      | VARCHAR(255) | คณะ                      | 'คณะศึกษาศาสตร์'                         |
| major        | VARCHAR(255) | สาขา                     | 'นวัตกรรม เทคโนโลยีและสื่อสารการศึกษา'   |
| logo_url     | TEXT         | URL โลโก้                | '/University_Logo/KKU_LOGO.png'          |
| color        | VARCHAR(7)   | สีประจำมหาวิทยาลัย       | '#FAB1A0'                                |
| order_index  | INTEGER      | ลำดับการแสดงผล           | 1, 2, 3, 4                               |
| is_visible   | BOOLEAN      | แสดง/ซ่อน                | true/false                               |
| created_at   | TIMESTAMP    | วันที่สร้าง              | auto                                     |
| updated_at   | TIMESTAMP    | วันที่อัพเดท             | auto                                     |

## 🎨 การแสดงผลบนเว็บ

### การ์ดมหาวิทยาลัย (เมื่อคลิกโลโก้)

แสดงข้อมูล:

- ชื่อมหาวิทยาลัย (ไทย/อังกฤษ)
- ภาค
- ปีการศึกษา
- ระดับการศึกษา (เช่น ปริญญาโท (M.Ed.))
- คณะ (ถ้ามี)
- สาขา (ถ้ามี)

### Tooltip (เมื่อ Hover โลโก้)

แสดงข้อมูล:

- ชื่อมหาวิทยาลัย (อังกฤษ)
- ภาค

## 🔧 การจัดการข้อมูล

### วิธีการแก้ไขข้อมูลมหาวิทยาลัย

#### ผ่าน SQL Editor:

```sql
UPDATE map_universities
SET
  name_th = 'ชื่อใหม่',
  degree_level = 'ปริญญาเอก (Ph.D.)',
  faculty = 'คณะใหม่',
  major = 'สาขาใหม่'
WHERE region = 'north';
```

#### ผ่าน Supabase Table Editor:

1. ไปที่ Table Editor
2. เลือกตาราง `map_universities`
3. คลิกแก้ไขแถวที่ต้องการ
4. บันทึก

### วิธีการเพิ่มมหาวิทยาลัยใหม่

```sql
INSERT INTO map_universities (
  region, name_th, name_en, region_th, year,
  degree_level, faculty, major, logo_url,
  color, order_index, is_visible
)
VALUES (
  'new_region',
  'มหาวิทยาลัยใหม่',
  'New University',
  'ภาคใหม่',
  '2025 - ปัจจุบัน',
  'ปริญญาตรี (B.Sc.)',
  'คณะวิทยาศาสตร์',
  'วิทยาการคอมพิวเตอร์',
  '/University_Logo/NEW_LOGO.png',
  '#FF6B6B',
  5,
  true
);
```

**หมายเหตุ:** ถ้าเพิ่มภาคใหม่ ต้องอัพเดทโค้ดด้วยเพื่อเพิ่ม:

- ตำแหน่งของ marker
- สีของภาคใน REGION_THEMES
- ข้อมูลภาคใน REGION_HIGHLIGHTS

### วิธีการซ่อน/แสดงมหาวิทยาลัย

```sql
-- ซ่อนมหาวิทยาลัย
UPDATE map_universities
SET is_visible = false
WHERE region = 'north';

-- แสดงมหาวิทยาลัย
UPDATE map_universities
SET is_visible = true
WHERE region = 'north';
```

## 🎯 ตัวอย่างการใช้งาน

### อัพเดทข้อมูลการศึกษาปัจจุบัน

```sql
-- อัพเดท KKU
UPDATE map_universities
SET
  year = '2025 - ปัจจุบัน',
  degree_level = 'ปริญญาโท (M.Ed.)',
  faculty = 'คณะศึกษาศาสตร์',
  major = 'นวัตกรรม เทคโนโลยีและสื่อสารการศึกษา'
WHERE region = 'northeast';

-- อัพเดท CMU (เตรียมเข้าศึกษา)
UPDATE map_universities
SET
  year = 'เตรียมเข้าศึกษา',
  degree_level = 'ปริญญาเอก (Ph.D.)',
  faculty = NULL,
  major = NULL
WHERE region = 'north';
```

## 🔍 การตรวจสอบและ Debug

### ตรวจสอบข้อมูลที่แสดงบนเว็บ

```sql
-- ดูเฉพาะมหาวิทยาลัยที่แสดง
SELECT region, name_th, degree_level, faculty, major, is_visible
FROM map_universities
WHERE is_visible = true
ORDER BY order_index;
```

### ตรวจสอบ Console ใน Browser

เปิด Developer Tools (F12) และดูที่ Console:

- ถ้ามี error "Error fetching universities" → ตรวจสอบ RLS policies
- ถ้าข้อมูลไม่แสดง → ตรวจสอบว่า `is_visible = true`

## 📝 Best Practices

1. **ใช้ degree_level ที่ชัดเจน:**

   - ✅ "ปริญญาโท (M.Ed.)"
   - ✅ "ปริญญาตรี (B.Sc.)"
   - ❌ "กำลังศึกษา"

2. **ระบุคณะและสาขาให้ครบถ้วน:**

   - faculty: "คณะศึกษาศาสตร์"
   - major: "นวัตกรรม เทคโนโลยีและสื่อสารการศึกษา"

3. **ใช้ year ที่เข้าใจง่าย:**

   - ✅ "2025 - ปัจจุบัน"
   - ✅ "2021 - 2024"
   - ✅ "เตรียมเข้าศึกษา"

4. **เก็บ logo_url ที่ถูกต้อง:**
   - ใช้ path relative: "/University_Logo/XXX.png"
   - ตรวจสอบว่าไฟล์มีอยู่จริง

## 🚨 การแก้ไขปัญหา

### ปัญหา: ข้อมูลไม่แสดงบนเว็บ

**วิธีแก้:**

1. ตรวจสอบ `is_visible = true`
2. ตรวจสอบ RLS policies
3. ตรวจสอบ Console สำหรับ errors
4. Refresh หน้าเว็บ (Ctrl+F5)

### ปัญหา: โลโก้ไม่แสดง

**วิธีแก้:**

1. ตรวจสอบ path ใน `logo_url`
2. ตรวจสอบว่าไฟล์มีอยู่ใน `/public/University_Logo/`
3. ตรวจสอบชื่อไฟล์ (case-sensitive)

### ปัญหา: ข้อมูลไม่อัพเดท

**วิธีแก้:**

1. Clear browser cache
2. Refresh หน้าเว็บ
3. ตรวจสอบว่า SQL UPDATE สำเร็จ
4. ตรวจสอบ `updated_at` timestamp

## 📚 เอกสารเพิ่มเติม

- `supabase_map_universities.sql` - SQL script สำหรับสร้างตาราง
- `MAP_SETTINGS_GUIDE.md` - คู่มือการตั้งค่าแผนที่
- `src/lib/supabase.ts` - TypeScript interfaces

## 🎉 สรุป

ตอนนี้ระบบแผนที่การศึกษาของคุณ:

- ✅ ดึงข้อมูลทั้งหมดจาก Database
- ✅ ไม่มี hardcoded data
- ✅ แก้ไขข้อมูลได้ง่ายผ่าน Supabase
- ✅ แสดงผลคณะและสาขาอย่างถูกต้อง
- ✅ รองรับการเพิ่ม/ลบ/แก้ไขมหาวิทยาลัย

ทุกการเปลี่ยนแปลงในฐานข้อมูลจะแสดงผลบนเว็บทันทีหลังจาก refresh! 🚀
